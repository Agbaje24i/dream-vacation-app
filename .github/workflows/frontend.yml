name: Frontend CI/CD

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Debug: Check if secrets are present
        run: |
          echo "DOCKER_HUB_USERNAME = ${{ secrets.DOCKER_HUB_USERNAME }}"
          echo "TOKEN present? ${{ secrets.DOCKER_HUB_TOKEN != '' }}"

      - name: Log in to Docker Hub
        run: echo "${{ secrets.DOCKER_HUB_TOKEN }}" | docker login -u "${{ secrets.DOCKER_HUB_USERNAME }}" --password-stdin

      - name: Build frontend image
        run: docker build -t ${{ secrets.DOCKER_HUB_USERNAME }}/dream-frontend:${{ github.sha }} ./frontend

      - name: Push frontend image
        run: docker push ${{ secrets.DOCKER_HUB_USERNAME }}/dream-frontend:${{ github.sha }}
